<h2>Testing! You have reached friend_request.html.erb</h2>

<table>
    <tr>
        <th>Email</th>
    </tr>
    <% @Users.each do |user| %>
        <% if current_user.email == user.email %>
        <% else %>
            <tr>
                <td><%= user.email %></td>
            </tr>
        <% end %>
    <% end %>
</table>